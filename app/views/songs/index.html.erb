<head>

</head>

<body>

  <h1>Songs</h1>
  <% @songs.each do |song| %>
    <h2><%= "#{song.title}" %></h2>
    <p><%= "Length: #{song.length}" %></p>
    <p><%= "Play Count: #{song.play_count}" %></p>
  <% end %>
</body>